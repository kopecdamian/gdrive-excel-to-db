# gdrive-excel-to-db
This program retrieves data from a spreadsheet using the Google Drive API and inserts it into a PostgreSQL table, ensuring no duplicate records are added.


# Installation
git clone https://github.com/kopecdamian/get-receipts.git
pip install -r requirements.txt
